= simple_form_for(@classroom) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :office_id, input_html: {value: @classroom.office_id || params[:office_id]}, as: :hidden

  .form-actions
    = f.button :submit
